<h1 class="text-center">Alpha Bloggers </h1>

<div class="container">
    <% @users.each do |user| %>
    <div class="row justify-content-md-center">
        <div class="col-8 mt-4 ">
            <div class="card text-center shadow mb-5 bg-body-tertiary rounded">
                <div class="card-header fst-italic">
                    <%= link_to user.username, user_path(user) %>
                </div>
                <div class="card-body">
                    <h5 class="card-title"><%= pluralize(user.articles.count ,"Article") %></h5>
                    <%= link_to 'View Articles', user_path(user), class:"btn btn-outline-success"%></a>
                </div>
                <div class="card-footer text-muted">
                    <small>joined <%=time_ago_in_words(user.created_at)%> ago</small>
                </div>
            </div>
        </div>
    </div>
    <% end %>
</div>

<div class="flickr_pagination ">
    <%= will_paginate @users, :container => false%>
</div>



